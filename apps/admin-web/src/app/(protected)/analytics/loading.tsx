import { AnalyticsSkeleton } from '@/shared/components/page-skeletons';

/**
 * Analytics Loading State
 * Exibido durante o carregamento da página de analytics
 */
export default function AnalyticsLoading() {
  return <AnalyticsSkeleton />;
}
import { TimeRecordsSkeleton } from '@/shared/components/page-skeletons';

/**
 * Time Records Loading State
 * Exibido durante o carregamento da página de registros de ponto
 */
export default function TimeRecordsLoading() {
  return <TimeRecordsSkeleton />;
}
import { ListPageSkeleton } from '@/shared/components/page-skeletons';

/**
 * Vacations Loading State
 * Exibido durante o carregamento da página de férias
 */
export default function VacationsLoading() {
  return <ListPageSkeleton />;
}
import { ListPageSkeleton } from '@/shared/components/page-skeletons';

/**
 * Audit Loading State
 * Exibido durante o carregamento da página de auditoria
 */
export default function AuditLoading() {
  return <ListPageSkeleton />;
}